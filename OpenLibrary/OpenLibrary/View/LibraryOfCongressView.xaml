<UserControl x:Class="OpenLibrary.View.LibraryOfCongressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModelWeb="clr-namespace:OpenLibrary.ViewModel.Web"
             xmlns:viewWeb="clr-namespace:OpenLibrary.View.Web">

    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/OpenLibrary;component/Resource/Style/OpenLibraryStyle.xaml" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- SRU Web Services -->
        <Border>
            <StackPanel>
                
                <TextBlock Text="SRU Web Services" 
                           Foreground="DimGray" 
                           FontSize="18"
                           Margin="5" />

                <ListView ItemsSource="{Binding WebServices}" Style="{StaticResource ListViewNoSelectStyle}" Margin="5">
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type viewModelWeb:WebServiceViewModel}">
                            <viewWeb:WebServiceView />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>
        
    </Grid>
</UserControl>
